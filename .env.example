# Sentinel environment configuration — copy to `.env` (git-ignored) and fill in.
# Only non-default / deployment-specific values need to be set; everything here
# has a working default in backend/config.py.
#
# Digital decode (the dsd-fme sidecar) needs NO configuration: the ingest secret
# is auto-generated by the backend and shared with the decoder via a Docker
# volume. To pin an explicit secret instead, uncomment and set this (compose
# passes it to both the backend and the decoder):
# SENTINEL_DECODER_SECRET=

# Sea domain — live vessels from AISStream.io (https://aisstream.io, free key).
# The key is only ever used server-side; it can also be entered in Settings › SEA,
# which takes precedence over this value. AISStream allows ONE connection per key,
# so don't run two Sentinel backends against the same key.
# AISSTREAM_API_KEY=

# Land domain — live feed credentials (traffic cameras / traffic data /
# webcams), for a headless deployment with no Settings UI access. JSON object
# keyed by feed id, e.g. {"tfl-jamcams": {"apiKey": "..."}}. A credential
# saved from Settings › LAND takes precedence over this value.
# LAND_FEED_CREDENTIALS_JSON=
