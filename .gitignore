tiles/
pmtiles
*.pmtiles
node_modules/
coverage/
.coverage
test-results/
.DS_Store
__pycache__/
*.py[cod]
*.db
*.db-shm
*.db-wal
.venv/

# Local environment / secrets (never commit) — see .env.example
.env
.env.*
!.env.example

# Decoder build artifacts / locally-compiled vocoder (never commit — patent)
decoder/build/
**/mbelib/
*.so
