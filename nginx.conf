server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # KILL CACHE for development
    add_header Last-Modified $date_gmt;
    add_header Cache-Control 'no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0';
    if_modified_since off;
    expires off;
    etag off;

    location ~* ^/assets/.*\.js\.map$ {
        return 204;
    }

    location /ws/ {
        proxy_pass http://sentinel-api:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }

    location / {
        proxy_pass http://sentinel-api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # Ensure the proxy doesn't cache either
        proxy_cache_bypass $http_upgrade;
        proxy_no_cache $http_upgrade;
    }
}


# NO HOT RELOAD

# server {
#     listen 80;

#     # Serve static files
#     root /usr/share/nginx/html;
#     index index.html;

#     # Suppress missing source maps for vendor assets
#     location ~* ^/assets/.*\.js\.map$ {
#         return 204;
#     }

#     # WebSocket proxy for SDR streaming
#     location /ws/ {
#         proxy_pass http://sentinel-api:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "Upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_read_timeout 3600s;
#         proxy_send_timeout 3600s;
#         proxy_connect_timeout 30s;
#         proxy_buffering off;
#     }

#     # Proxy everything else to FastAPI — it owns all routes (pages + API + static assets)
#     location / {
#         proxy_pass http://sentinel-api:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header Origin "";
#         proxy_set_header Referer "";
#     }
# }
